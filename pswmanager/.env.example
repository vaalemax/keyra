# .env

# --- Database -----------------------------------------------------
DB_USERNAME=user
DB_PASSWORD=user
DB_URL=jdbc:postgresql://localhost:5432/pswmanager
# --- Session cookie -------------------------------------------------
# Set to true when serving the app over HTTPS (required in production).
COOKIE_SECURE=false